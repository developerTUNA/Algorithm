#pragma warning (disable:4996)
#include <stdio.h>

int Problem02558(void)
{
    int nA = 0;
    int nB = 0;
    int nResult = 0;

    scanf("%d %d", &nA, &nB);
    nResult = nA + nB;
    printf("%d\n", nResult);
    return 0;
}